<Window x:Class="HaloVision.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="HaloVision Demo" Height="260" Width="560">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBox Grid.Row="0" Grid.Column="0" Margin="0,0,8,8" Text="{Binding ImagePath, UpdateSourceTrigger=PropertyChanged}"/>
        <Button Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" Padding="16,6" Content="选择图片" Command="{Binding SelectImageCommand}"/>

        <Button Grid.Row="1" Grid.ColumnSpan="2" Margin="0,0,0,8" Padding="16,6" Content="执行检测" Command="{Binding InspectCommand}"/>

        <TextBlock Grid.Row="2" Grid.ColumnSpan="2" Margin="0,0,0,8" FontWeight="SemiBold" Text="{Binding Status}"/>

        <ProgressBar Grid.Row="3" Grid.ColumnSpan="2" Height="24" Maximum="1" Value="{Binding Score, Mode=OneWay}"/>
    </Grid>
</Window>
